# راهنمای کامل دستورات — سایت MobileGSM

همه‌ی دستوراتی که برای اجرا، بررسی و دیپلوی این سایت لازم دارید، با توضیح.

---

## ۱. بررسی نصب بودن Node.js

```bash
node --version
```
**توضیح:** نسخه‌ی Node.js نصب‌شده روی سیستم رو نشون می‌ده (مثلاً `v24.14.1`). اگه پیام خطا داد یعنی Node نصب نیست و باید از nodejs.org دانلودش کنید.

---

## ۲. رفتن به پوشه‌ی پروژه

```bash
cd website
```
**توضیح:** وارد پوشه‌ای می‌شه که فایل‌های سایت (`server.js`, `index.html` و بقیه) توشه. اگه پوشه جای دیگه‌ایه، مسیرش رو به‌جای `website` بنویسید (مثلاً `cd Downloads/website`).

---

## ۳. روشن کردن سرور (حالت عادی)

```bash
node server.js
```
**توضیح:** سرور رو روی پورت پیش‌فرض `3000` روشن می‌کنه. بعدش باید مرورگر رو باز کنید و برید به:
```
http://localhost:3000
```
**نکته:** تا وقتی این دستور در حال اجراست (پنجره‌ی ترمینال باز و روشن)، سایت در دسترسه. با بستن ترمینال یا زدن `Ctrl + C`، سرور خاموش می‌شه.

---

## ۴. روشن کردن سرور روی یک پورت دلخواه

```bash
PORT=4000 node server.js
```
**توضیح:** به‌جای پورت پیش‌فرض `3000`، سرور رو روی `4000` بالا می‌آره (یا هر عدد دیگه‌ای که بذارید). مفید وقتی پورت ۳۰۰۰ توسط برنامه‌ی دیگه‌ای اشغال شده.

*(در ویندوز با Command Prompt به‌جای این دستور باید بنویسید:)*
```cmd
set PORT=4000 && node server.js
```

---

## ۵. اجرای سرور با دستور جایگزین (از طریق package.json)

```bash
npm start
```
**توضیح:** دقیقاً همون کار `node server.js` رو انجام می‌ده؛ فقط یک میان‌بر تعریف‌شده در `package.json` هست.

---

## ۶. خاموش کردن سرور

```
Ctrl + C
```
**توضیح:** توی همون پنجره‌ی ترمینالی که سرور توش روشنه بزنید. سرور بلافاصله متوقف می‌شه.

---

## ۷. دیدن اطلاعات ذخیره‌شده (فایل‌های داده)

اینا دستور نیستن، فقط فایل‌هایی هستن که با هر ویرایشگر متنی (Notepad, VS Code) می‌تونید بازشون کنید:

| فایل | محتواش چیه |
|---|---|
| `data/users.json` | همه‌ی کاربران ثبت‌نام‌شده |
| `data/orders.json` | همه‌ی سفارش‌های ثبت‌شده |
| `data/carts.json` | سبد خرید هر کاربر |
| `data/wishlists.json` | علاقه‌مندی‌های هر کاربر |
| `data/reviews.json` | نظرات ثبت‌شده روی محصولات |
| `data/sessions.json` | کاربرانی که الان لاگین هستن |

**نکته:** این پوشه (`data/`) اولین باری که سرور رو اجرا کنید، خودش خودکار ساخته می‌شه؛ نیازی نیست دستی بسازیدش.

---

## ۸. بررسی سالم بودن فایل‌های جاوااسکریپت (اختیاری، برای دیباگ)

```bash
node --check server.js
```
**توضیح:** فقط چک می‌کنه کد اون فایل از نظر نحوی (syntax) درسته یا نه، بدون اینکه واقعاً اجراش کنه. اگه چیزی رو خودتون دستی ویرایش کردید و سایت بالا نیومد، این دستور کمک می‌کنه بفهمید مشکل کجاست.

---

## ۹. دستورات مخصوص دیپلوی روی Liara (یا پلتفرم‌های مشابه)

اینا داخل پنل همون سرویس (نه ترمینال شخصی شما) اجرا می‌شن، ولی برای یادآوری:

```bash
git init
git add .
git commit -m "first deploy"
git push liara main
```
**توضیح:** پروژه رو به‌صورت یک مخزن Git آماده می‌کنه و به آدرسی که پلتفرم بهتون می‌ده (`liara` در این مثال) push می‌کنه تا آپلود و اجرا بشه. دستور دقیق رو معمولاً خود پنل همون سرویس بهتون نشون می‌ده.

---

## خلاصه‌ی روزمره (چیزی که هر بار لازم دارید)

```bash
cd website
node server.js
```
بعد مرورگر رو باز کنید → `http://localhost:3000`
